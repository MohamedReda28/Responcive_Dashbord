import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF6C5DD3);       // اللون الرئيسي (بنفسجي غني)
  static const Color secondary = Color(0xFFFF6B6B);     // لون ثانوي (وردي مرجاني)

  static const Color background = Color(0xFFFDF6EC);    // الخلفية العامة (بيج دافئ)
  static const Color cardBackground = Color(0xFFFFF0EB); // خلفية البطاقات (وردي فاتح)

  static const Color success = Color(0xFF38A169);       // نجاح/أرباح (أخضر)
  static const Color danger = Color(0xFFDD6B20);        // خسارة/مصاريف (برتقالي محروق)

  static const Color textPrimary = Color(0xFF2D2D2D);   // نص أساسي (رمادي غامق)
  static const Color textSecondary = Color(0xFF6E6E6E); // نص ثانوي (رمادي باهت)

  static const Color accent = Color(0xFFFFE66D);        // لون مساعد (أصفر ناعم)
}
